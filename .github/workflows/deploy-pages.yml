name: Deploy site to Cloudflare Pages

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'site/**'
      - 'scripts/qa.py'
      - 'scripts/build_locales.py'
      - 'scripts/build_locales_uk_ru.py'
      - 'scripts/build_locales_hi_bn.py'
      - 'scripts/build_route_locales.py'
      - 'scripts/build_route_locales_ru.py'
      - 'scripts/build_living_locales.py'
      - 'scripts/build_remaining_locales.py'
      - 'scripts/build_structural_parity_locales.py'
      - 'scripts/check_locale_parity.py'
      - 'scripts/finalize_live_locales.py'
      - 'scripts/build_sitemap.py'
      - '.github/workflows/deploy-pages.yml'

permissions:
  contents: read
  deployments: write

concurrency:
  group: cloudflare-pages-production
  cancel-in-progress: true

jobs:
  qa-and-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      CF_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CF_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - run: pip install beautifulsoup4
      - name: Build staged French and Spanish translations
        run: python scripts/build_locales.py
      - name: Build staged Ukrainian and Russian translations
        run: python scripts/build_locales_uk_ru.py
      - name: Build staged Hindi and Bengali translations
        run: python scripts/build_locales_hi_bn.py
      - name: Build staged migration route translations
        run: python scripts/build_route_locales.py
      - name: Build native Russian migration route translations
        run: python scripts/build_route_locales_ru.py
      - name: Build staged living-in-Portugal translations
        run: python scripts/build_living_locales.py
      - name: Build remaining staged translations
        run: python scripts/build_remaining_locales.py
      - name: Rebuild staged locales from complete Portuguese page structure
        run: python scripts/build_structural_parity_locales.py
      - name: Check full page and feature parity before locale publication
        run: python scripts/check_locale_parity.py
      - name: Finalize published translations
        run: python scripts/finalize_live_locales.py
      - name: Build multilingual sitemap
        run: python scripts/build_sitemap.py
      - name: Run site QA
        run: python scripts/qa.py
      - name: Cloudflare secrets not configured yet
        if: env.CF_API_TOKEN == '' || env.CF_ACCOUNT_ID == ''
        run: echo 'QA passed. Deployment waits for Cloudflare secrets.'
      - name: Deploy
        if: env.CF_API_TOKEN != '' && env.CF_ACCOUNT_ID != ''
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy site --project-name=guia-migrante-pt
          gitHubToken: ${{ secrets.GITHUB_TOKEN }}
